<!DOCTYPE html>
<html lang="en">
<%- include('./head') %>
<body>
  <h2>회원 관리</h2>

  <% if(data){ %>
    <p><%= data.name %>님 환영합니다.</p>
  <% } else { %>
    <p>아직 로그인을 하지 않으셨군요!</p>
  <% } %>

  <div>
    <a href="/signup">회원가입</a>&nbsp;
    <a href="/signin">로그인</a>&nbsp;
    <a href="/users">회원목록</a>&nbsp;
    <% if(data){ %>
      <a href="/profile">내 프로필</a>&nbsp;
    <% } %>
  </div>
</body>
</html>